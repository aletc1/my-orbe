{
  "setup-worktree": [
    "pnpm install",
    "cp -n .env.example ./apps/api/.env",
    "sed -i.bak \"s|^MOCK_GOOGLE_AUTH_USER=.*|MOCK_GOOGLE_AUTH_USER=$(git config user.email)|\" ./apps/api/.env && rm -f ./apps/api/.env.bak"
  ]
}